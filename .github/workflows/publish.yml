name: Deploy WebGen Site Publisher

on:
  push:
    branches:
      - main
    paths:
      - 'requests/**'
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write 

    steps:
      - name: Checkout repository (Read files using PAT)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.DUMMY_GITHUB_PAT }} 

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies (requests, glob)
        run: pip install requests

      # 🚀 تشغيل السكربت الرئيسي
      - name: Run Publisher Script
        run: python .github/workflows/api_publisher.py
        env:
          # تمرير المفتاح السري لحساب GenAI210
          DUMMY_GITHUB_PAT: ${{ secrets.DUMMY_GITHUB_PAT }}
          # 🚨 التصحيح الحاسم: تعيين المالك بشكل ثابت إلى 'GenAI210' لإنشاء المستودع تحته
          REPO_OWNER: "GenAI210"

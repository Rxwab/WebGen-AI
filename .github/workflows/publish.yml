name: Deploy WebGen Site Publisher

on:
  # 🚨 التعديل الحاسم هنا: هذا السطر يراقب أي ملف جديد يتم إنشاؤه في مجلد 'requests'
  push:
    branches:
      - main  # أو اسم الفرع الرئيسي الذي تستخدمه
    paths:
      - 'requests/**'
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    # الصلاحيات المطلوبة للسماح بإنشاء المستودعات
    permissions:
      contents: write 

    steps:
      - name: Checkout repository (Read files using PAT)
        uses: actions/checkout@v4
        # تمرير الـ token هنا يضمن أن الـ Action يمكنه قراءة الملفات التي تم رفعها للتو
        with:
          token: ${{ secrets.DUMMY_GITHUB_PAT }} 

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies (requests, glob)
        run: pip install requests

      # 🚀 تشغيل السكربت الرئيسي
      - name: Run Publisher Script
        run: python .github/workflows/api_publisher.py
        env:
          # تمرير المفتاح القوي (الناشر)
          DUMMY_GITHUB_PAT: ${{ secrets.DUMMY_GITHUB_PAT }}
          # تمرير اسم المستخدم (Rxwab) بشكل ديناميكي
          REPO_OWNER: ${{ github.repository_owner }}

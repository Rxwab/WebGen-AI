
name: Deploy WebGen Site

on:
  # 🚀 هذا هو التعديل المطلوب: تم تغيير المسار ليراقب التغييرات في مجلد 'requests'
  push:
    paths:
      - 'requests/**' # **هذا السطر هو المفتاح**

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    # الصلاحيات المطلوبة لتشغيل السكربت (للسماح بإنشاء مستودع جديد)
    permissions:
      contents: write # مطلوب للسماح لـ api_publisher.py بإنشاء مستودع جديد

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      # 🚨 ملاحظة هامة: يجب أن يكون الملف api_publisher.py موجوداً في مجلد .github/workflows/
      - name: Run Publisher Script
        run: python .github/workflows/api_publisher.py
        env:
          # تمرير المفتاح الوهمي (الناشر) من Secrets
          DUMMY_GITHUB_PAT: ${{ secrets.DUMMY_GITHUB_PAT }}
          # تمرير اسم المستخدم الأساسي لتوليد الروابط
          REPO_OWNER: 'Rxwab'

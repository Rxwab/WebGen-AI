name: Deploy WebGen Site Publisher

on:
  push:
    branches:
      - main  # تأكد من أنه اسم فرعك الرئيسي
    paths:
      - 'requests/**'
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    # الصلاحيات مطلوبة للسماح بإنشاء المستودعات
    permissions:
      contents: write 

    steps:
      - name: Checkout repository (Read files using PAT)
        uses: actions/checkout@v4
        # نستخدم مفتاح الحساب الناشر لقراءة الملف الذي تم رفعه للتو
        with:
          token: ${{ secrets.DUMMY_GITHUB_PAT }} 

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies (requests, glob)
        run: pip install requests

      # 🚀 تشغيل السكربت الرئيسي
      - name: Run Publisher Script
        run: python .github/workflows/api_publisher.py
        env:
          # تمرير المفتاح السري للحساب الناشر (GenAI210)
          DUMMY_GITHUB_PAT: ${{ secrets.DUMMY_GITHUB_PAT }}
          # 🚨 التعديل الحاسم: تعيين المالك بشكل ثابت إلى 'GenAI210'
          REPO_OWNER: "GenAI210"
